import{requirePermission}from"@/lib/community";import{createClient}from"@/lib/supabase/server";import{AdminHeader,DataTable}from"../_components";import{manageTaxonomyAction}from"./actions";
export default async function Page(){await requirePermission("categories.manage");const s=await createClient();const[{data:categories},{data:locations}]=await Promise.all([s.from("categories").select("slug,name,sort_order,archived_at,subcategories(count)").order("sort_order"),s.from("locations").select("slug,label,sort_order,archived_at").order("sort_order")]);return <main><AdminHeader title="Taxonomia e locais" description="Categorias, subcategorias e locais são configuráveis; itens usados são arquivados, nunca apagados fisicamente."/><form action={manageTaxonomyAction} className="mt-6 grid gap-3 rounded-xl border bg-white p-5 sm:grid-cols-2"><select name="entity" className="min-h-11 rounded-lg border px-3"><option value="category">Categoria</option><option value="subcategory">Subcategoria</option><option value="location">Local</option></select><select name="action" className="min-h-11 rounded-lg border px-3"><option value="create">Criar</option><option value="update">Atualizar</option><option value="archive">Arquivar</option><option value="restore">Restaurar</option></select><input name="slug" required pattern="[a-z0-9-]+" className="min-h-11 rounded-lg border px-3" placeholder="slug-estavel"/><input name="name" className="min-h-11 rounded-lg border px-3" placeholder="Nome/rótulo"/><input name="sort_order" type="number" min="0" className="min-h-11 rounded-lg border px-3" placeholder="Ordem"/><input name="category_slug" className="min-h-11 rounded-lg border px-3" placeholder="Slug da categoria (para subcategoria)"/><button className="rounded-lg bg-forest p-3 font-bold text-white sm:col-span-2">Aplicar sem hard delete</button></form><DataTable headers={["Categoria","Slug","Ordem","Estado"]} rows={(categories??[]).map(x=>[x.name,x.slug,x.sort_order,x.archived_at?"Arquivada":"Ativa"])}/><h2 className="mt-8 text-xl font-bold">Locais</h2><DataTable headers={["Local","Slug","Ordem","Estado"]} rows={(locations??[]).map(x=>[x.label,x.slug,x.sort_order,x.archived_at?"Arquivado":"Ativo"])}/></main>}
