import { ReauthenticateButton } from "@/components/reauthenticate-button";
import { requirePermission } from "@/lib/community";
import { createClient } from "@/lib/supabase/server";
import { AdminHeader, AdminEmpty, DataTable } from "../_components";
import { verifyOfficialAction } from "./actions";

const input="min-h-11 rounded-lg border border-[#aaa59e] bg-white px-3";
export default async function Page(){await requirePermission("official_accounts.verify");const s=await createClient();const{data}=await s.from("official_representations").select("id,user_id,role_type,organization,status,starts_at,ends_at").order("starts_at",{ascending:false});return <main><AdminHeader title="Contas oficiais" description="Representação oficial válida permite resposta e status operacional oficiais, mas nunca administração ou moderação."/><div className="mt-5"><ReauthenticateButton nextPath="/admin/contas-oficiais"/></div><form action={verifyOfficialAction} className="mt-6 grid gap-3 rounded-xl border bg-white p-5 sm:grid-cols-2"><h2 className="text-lg font-bold sm:col-span-2">Verificar representação</h2><input className={input} name="user_id" required placeholder="UUID do usuário"/><select className={input} name="role_type"><option value="manager">Síndico</option><option value="deputy_manager">Subsíndico</option><option value="council">Conselho</option><option value="administrator">Administradora</option><option value="authorized_representative">Representante autorizado</option><option value="authorized_provider">Prestador autorizado</option></select><input className={input} name="organization" placeholder="Organização"/><input className={input} type="datetime-local" name="starts_at" required/><input className={input} type="datetime-local" name="ends_at"/><input className={input} name="reason" required minLength={5} placeholder="Justificativa da verificação"/><button className="min-h-11 rounded-lg bg-forest px-4 font-bold text-white sm:col-span-2">Verificar conta oficial</button></form>{data?.length?<DataTable headers={["Organização","Tipo","Estado","Início"]} rows={data.map(x=>[x.organization??"—",x.role_type,x.status,new Date(x.starts_at).toLocaleDateString("pt-BR")])}/>:<AdminEmpty>Nenhuma representação oficial registrada.</AdminEmpty>}</main>}
