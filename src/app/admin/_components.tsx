export function AdminHeader({title,description}:{title:string;description:string}){return <header className="border-b border-[#ddd6ce] pb-5"><p className="text-xs font-bold uppercase tracking-[.16em] text-forest">Governança LASTRO</p><h1 className="mt-1 text-3xl font-bold">{title}</h1><p className="mt-2 max-w-2xl text-[#64665f]">{description}</p></header>}
export function AdminEmpty({children}:{children:React.ReactNode}){return <div className="mt-6 rounded-xl border border-dashed border-[#bdb5ac] bg-white p-8 text-center text-[#62645e]">{children}</div>}
export function DataTable({headers,rows}:{headers:string[];rows:React.ReactNode[][]}){return <div className="mt-6 overflow-x-auto rounded-xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-sand"><tr>{headers.map(h=><th key={h} className="px-4 py-3">{h}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i} className="border-t">{r.map((v,j)=><td key={j} className="px-4 py-3">{v}</td>)}</tr>)}</tbody></table></div>}
