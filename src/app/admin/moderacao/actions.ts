"use server";
import{revalidatePath}from"next/cache";import{z}from"zod";import{requirePermission}from"@/lib/community";import{createClient}from"@/lib/supabase/server";
export async function applyModerationAction(fd:FormData){await requirePermission("moderation.review");const d=z.object({flag_id:z.string().uuid(),action:z.enum(["no_action","restrict","hide","request_correction","restore","archive","remove_file","other"]),reason:z.string().trim().min(5).max(1000)}).parse(Object.fromEntries(fd));const s=await createClient();const{error}=await s.rpc("apply_moderation_action",{p_flag_id:d.flag_id,p_action:d.action,p_reason:d.reason});if(error)throw new Error(error.message);revalidatePath("/admin/moderacao")}
export async function revertModerationAction(fd:FormData){await requirePermission("moderation.restore");const d=z.object({action_id:z.string().uuid(),reason:z.string().trim().min(5)}).parse(Object.fromEntries(fd));const s=await createClient();const{error}=await s.rpc("revert_moderation_action",{p_action_id:d.action_id,p_reason:d.reason});if(error)throw new Error(error.message);revalidatePath("/admin/moderacao")}
export async function decideReviewAction(fd:FormData){await requirePermission("moderation.review");const d=z.object({request_id:z.string().uuid(),state:z.enum(["upheld","reversed","partially_reversed"]),reason:z.string().trim().min(5)}).parse(Object.fromEntries(fd));const s=await createClient();const{error}=await s.rpc("decide_review_request",{p_request_id:d.request_id,p_state:d.state,p_reason:d.reason});if(error)throw new Error(error.message);revalidatePath("/admin/moderacao")}
