"use server";
import{revalidatePath}from"next/cache";import{z}from"zod";import{requirePermission}from"@/lib/community";import{createClient}from"@/lib/supabase/server";
const merge=z.object({source_issue_id:z.string().uuid(),target_issue_id:z.string().uuid(),reason:z.string().trim().min(5).max(500)});
export async function mergeIssuesAction(fd:FormData){await requirePermission("issues.merge");const d=merge.parse(Object.fromEntries(fd));const s=await createClient();const{error}=await s.rpc("merge_issues",{p_source_issue_id:d.source_issue_id,p_target_issue_id:d.target_issue_id,p_reason:d.reason});if(error)throw new Error(error.message);revalidatePath("/admin/problemas")}
export async function undoMergeAction(fd:FormData){await requirePermission("issues.merge");const d=z.object({merge_id:z.string().uuid(),reason:z.string().trim().min(5)}).parse(Object.fromEntries(fd));const s=await createClient();const{error}=await s.rpc("undo_issue_merge",{p_merge_id:d.merge_id,p_reason:d.reason});if(error)throw new Error(error.message);revalidatePath("/admin/problemas")}
export async function splitReportAction(fd:FormData){await requirePermission("issues.split");const d=z.object({report_id:z.string().uuid(),title:z.string().trim().min(8).max(180),reason:z.string().trim().min(5)}).parse(Object.fromEntries(fd));const s=await createClient();const{error}=await s.rpc("split_report_to_new_issue",{p_report_id:d.report_id,p_title:d.title,p_reason:d.reason});if(error)throw new Error(error.message);revalidatePath("/admin/problemas")}
export async function undoSplitAction(fd:FormData){await requirePermission("issues.split");const d=z.object({split_id:z.string().uuid(),reason:z.string().trim().min(5)}).parse(Object.fromEntries(fd));const s=await createClient();const{error}=await s.rpc("undo_issue_split",{p_split_id:d.split_id,p_reason:d.reason});if(error)throw new Error(error.message);revalidatePath("/admin/problemas")}
