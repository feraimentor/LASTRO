import{ReauthenticateButton}from"@/components/reauthenticate-button";import{requirePermission}from"@/lib/community";import{createClient}from"@/lib/supabase/server";import{AdminHeader,AdminEmpty}from"../_components";import{decidePrivacyAction}from"./actions";
export default async function Page(){await requirePermission("private_data.view");await requirePermission("settings.manage");const s=await createClient();const{data}=await s.from("privacy_requests").select("id,user_id,request_type,details,state,created_at").in("state",["received","validating","in_progress"]).order("created_at");return <main><AdminHeader title="Solicitações de privacidade" description="Cada pedido recebe análise contextual e decisão auditada; a interface não executa exclusão irrestrita."/><div className="mt-5"><ReauthenticateButton nextPath="/admin/privacidade"/></div><div className="mt-6 space-y-4">{data?.length?data.map(request=><form key={request.id} action={decidePrivacyAction} className="grid gap-3 rounded-xl border bg-white p-5"><b>{request.request_type} · usuário {request.user_id}</b><p>{request.details}</p><input type="hidden" name="request_id" value={request.id}/><select name="state" className="min-h-11 rounded-lg border px-3"><option value="completed">Concluído</option><option value="partially_completed">Parcialmente concluído</option><option value="denied">Negado com fundamento</option></select><textarea name="decision" required minLength={10} className="min-h-24 rounded-lg border p-3" placeholder="Decisão comunicável ao titular"/><input name="reason" required minLength={5} className="min-h-11 rounded-lg border px-3" placeholder="Fundamento interno mínimo"/><button className="rounded-lg bg-forest p-3 font-bold text-white">Registrar decisão</button></form>):<AdminEmpty>Nenhuma solicitação pendente.</AdminEmpty>}</div></main>}
