import Link from"next/link";import{PageHeader}from"@/components/community-shell";import{requireCommunityUser}from"@/lib/community";import{createClient}from"@/lib/supabase/server";import{signOutAction}from"./actions";
export default async function ProfilePage(){const u=await requireCommunityUser();const s=await createClient();const[{data:p},{data:links}]=await Promise.all([s.from("profiles").select("display_name,first_name,last_name,relation_to_condo,public_identity_preference,access_state").eq("id",u.id).single(),s.from("resident_unit_links").select("relation_type,condo_units(unit_label,blocks(label))").eq("user_id",u.id).is("ended_at",null)]);return <main className="mx-auto max-w-4xl px-4 py-8 sm:px-6"><PageHeader eyebrow="Conta verificada" title={p?.display_name??"Meu perfil"} description="Dados privados e identidade pública são tratados separadamente."/><section className="mt-7 rounded-2xl bg-white p-6"><dl className="grid gap-5 sm:grid-cols-2"><div><dt className="text-sm text-[#686a63]">Nome</dt><dd className="font-semibold">{p?.first_name} {p?.last_name}</dd></div><div><dt className="text-sm text-[#686a63]">Preferência de identidade</dt><dd className="font-semibold">{p?.public_identity_preference}</dd></div><div><dt className="text-sm text-[#686a63]">Vínculo</dt><dd className="font-semibold">{p?.relation_to_condo}</dd></div><div><dt className="text-sm text-[#686a63]">Estado de acesso</dt><dd className="font-semibold">{p?.access_state}</dd></div></dl><p className="mt-5 text-sm">Vínculos ativos: {links?.length??0}</p></section><div className="mt-5 flex flex-wrap gap-3"><Link href="/perfil/privacidade" className="rounded-lg border border-forest px-4 py-2 font-semibold text-forest">Privacidade e LGPD</Link><form action={signOutAction}><button className="rounded-lg border border-[#aaa39a] px-4 py-2 font-semibold">Sair</button></form></div></main>}
