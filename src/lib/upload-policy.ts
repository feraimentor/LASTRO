const mimeExtensions: Record<string, string[]> = { "image/jpeg":["jpg","jpeg"],"image/png":["png"],"image/webp":["webp"],"application/pdf":["pdf"],"audio/mpeg":["mp3"],"audio/mp4":["m4a","mp4"],"video/mp4":["mp4"],"application/vnd.openxmlformats-officedocument.wordprocessingml.document":["docx"],"application/vnd.openxmlformats-officedocument.spreadsheetml.sheet":["xlsx"] };
export function validateUploadMetadata(file:{name:string;type:string;size:number},maxBytes=52_428_800){const ext=file.name.split(".").pop()?.toLowerCase()??"";if(file.size<=0||file.size>maxBytes)return{ok:false as const,reason:"invalid_size"};if(!mimeExtensions[file.type]?.includes(ext))return{ok:false as const,reason:"mime_extension_mismatch"};return{ok:true as const}}
export function evidencePath(userId:string,sha256:string,extension:string){if(!/^[0-9a-f]{64}$/.test(sha256))throw new Error("invalid_sha256");return `${userId}/${crypto.randomUUID()}/${sha256}.${extension.toLowerCase()}`}
