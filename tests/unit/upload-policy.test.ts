import{describe,expect,it}from"vitest";import{validateUploadMetadata,evidencePath}from"@/lib/upload-policy";describe("evidence upload policy",()=>{it("rejects mismatched extension",()=>expect(validateUploadMetadata({name:"evidence.exe",type:"application/pdf",size:100}).ok).toBe(false));it("builds a PII-free immutable path",()=>{const p=evidencePath("user-id","a".repeat(64),"pdf");expect(p).toMatch(/^user-id\/[0-9a-f-]+\/[a-f0-9]{64}\.pdf$/)})});
