import{describe,expect,it,vi}from"vitest";import{correlationId,logEvent,sanitizeLogMetadata}from"@/lib/observability";
describe("observabilidade segura",()=>{it("remove segredos e valores complexos",()=>{expect(sanitizeLogMetadata({token:"x",email:"pessoa@example.invalid",count:2,nested:{pii:true},safe:"ok"})).toEqual({count:2,safe:"ok"})});it("aceita somente correlation id UUID",()=>{const valid="019fe737-f5bd-72d3-90e4-9fa8f6c1e73e";expect(correlationId(new Request("https://example.invalid",{headers:{"x-request-id":valid}}))).toBe(valid);expect(correlationId(new Request("https://example.invalid",{headers:{"x-request-id":"token-invalido"}}))).not.toBe("token-invalido")});it("gera JSON estruturado sem conteúdo proibido",()=>{const spy=vi.spyOn(console,"error").mockImplementation(()=>undefined);const record=logEvent("error",{correlationId:"019fe737-f5bd-72d3-90e4-9fa8f6c1e73e",route:"/teste",operation:"falha",errorCode:"safe_code",metadata:{password:"x",attempt:1}});expect(record.metadata).toEqual({attempt:1});expect(spy).toHaveBeenCalledOnce();spy.mockRestore()})});
